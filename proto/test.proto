syntax = "proto3";
package txwx;

message WeatherMessage {
  enum Type {
    METAR = 0;
    TAF = 1;
    BEACON = 2;
  }
  Type type = 1;
  uint32 tx_time = 2;		// UNIXTIME.
  float station_lat = 3;
  float station_lng = 4;
  uint32 observation_time = 5; // UNIXTIME.

  string text_data = 10;
}
